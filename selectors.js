let l = [180, 190, 165, 123, 234];

let state = {
    taille_choisie : 0
};

// CODE : mettre dans taille_choisie dans state, la valeur de l en index 2
state.taille_choisie = l[2];

console.log(state);